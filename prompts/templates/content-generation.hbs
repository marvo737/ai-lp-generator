## 応答フォーマットの定義:
あなたは必ず以下のJSON構造で応答してください。他のテキストは一切含めないでください。

```json
{
  "mdxContent": "ここに生成されたMDXコンテンツの文字列が入ります。MDXの生成が不要な場合や、ユーザーへの質問が主目的である場合は、nullを設定してください。",
  "chatResponse": "ユーザーへの確認事項や質問、生成完了の報告など、チャットUIに表示すべきテキストメッセージが入ります。"
}
```

## ユーザーリクエスト:
{{userPrompt}}

## コンテンツ生成の指示:
上記の応答フォーマット定義に従い、以下のmdxテンプレート構造とスキーマ定義を完全に理解し、`mdxContent`キーに新しいコンテンツを生成してください。

### 重要なポイント:
- 各ブロックの内容を創造的に書き換えてください
- mdxの構造は絶対に変更しないでください
- 各ブロックのフィールド定義（フィールド名、型、階層）は厳密に保持してください
- 必須フィールドを省略してはいけません
- スキーマに存在しないフィールドを追加してはいけません
- `actions` オブジェクト内の `link` フィールドには、必ず `/` などの有効なURLパス文字列を設定し、`null` や空文字列にしてはいけません。
- `stats` ブロックの `label` フィールドには、項目名（例：「満足度」）のみを記述し、説明的な文章や単位（「%」など）は含めないでください。
- `stats` ブロックの `value` フィールドは、可能な限り数値として記述してください（例: `95`）。文字列（例: `"95%"`）は避けてください。

---mdxテンプレートここから---
{{mdxTemplate}}
---mdxテンプレートここまで---

---ブロックスキーマ定義ここから---
{{schemaMarkdown}}
---ブロックスキーマ定義ここまで---

{{#if examples}}
## 参考例:
{{#each examples}}
### {{@key}}の例:
```json
{{this}}
```
{{/each}}
{{/if}}

以上の全ての指示に厳密に従って、指定されたテーマに沿ったコンテンツを生成し、定義されたJSONフォーマットで応答してください。